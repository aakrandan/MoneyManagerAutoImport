# MoneyManagerAutoImport
Upload a bank statement PDF, auto-categorize transactions using your Money Manager history, and download a ready-to-import .xlsx/.tsv file. Runs entirely in the browser — no server, no login required.
